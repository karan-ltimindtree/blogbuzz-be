import { createCommentValidator } from './createComment.validator';
import { getCommentsByPostValidator } from './getCommentsByPost.validator';

const CommentsValidator = { createCommentValidator, getCommentsByPostValidator };
export default CommentsValidator;
