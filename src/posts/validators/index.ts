import { createPostValidator } from './createPost.validator';
import { getSinglePostValidator } from './getSinglePost.validator';

const PostValidator = { createPostValidator, getSinglePostValidator };
export default PostValidator;
