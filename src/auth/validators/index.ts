import { loginValidator } from './login.validator';
import { registerValidator } from './resgiter.validator';

const AuthValidator = { loginValidator, registerValidator };

export default AuthValidator;
